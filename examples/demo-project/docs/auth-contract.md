# Authentication Contract

Authentication accepts an account identifier and returns a session.

```driftlock-index
{
  "schema_version": 2,
  "id": "demo-auth-contract",
  "authority_key": "demo.auth.contract",
  "level": 2,
  "role": "contract",
  "lifecycle_status": "active",
  "watch_paths": [
    "src/auth/**/*.py"
  ]
}
```
